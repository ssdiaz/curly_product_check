#!/usr/bin/env ruby

require "bundler/setup" #We require ‘bundler/setup’, otherwise app won’t know to use bundler to install our gems and then our Gemfile would be pointless
require "curly_product_check"

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

# (If you use this, don't forget to add pry to your Gemfile!)
# require "pry"
# Pry.start

require "irb"
IRB.start(__FILE__)
